
spring:
  spring:
    config:
      activate:
        on-profile: dev
  server:
    port: 8080

  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/test
    username: root
    password: Philip$!
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: 'create-drop'
